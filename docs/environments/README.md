# Environments

Use this section to document how RematePOS is separated across environments such as `develop`, `qa`, and `main`.

Include notes about environment variables, deployment values, and the differences expected between each stage.
