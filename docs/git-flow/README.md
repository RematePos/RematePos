# Git Flow

Use this section for the branch and pull request workflow used in RematePOS.

Document the feature branch format `feature/HU-XXX-INICIALES-description`, conventional commit messages in English, and the rule that pull requests target `develop` unless a different flow is explicitly approved.
